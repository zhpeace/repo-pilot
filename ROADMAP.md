# RepoPilot 路线图

> 本文件记录待实现的功能与方向，供后续开发参考。当前状态以 v0.2.0 为基础。

## 待实现

### 1. 云同步（多设备同步配置）⭐ 已确认，后续实现

**目标**：让用户在多台电脑（公司/家里）间同步本地配置——根目录列表（state.json）、分组（groups.json）、收藏（favs.json）。

**核心思路**：同步的是"配置 JSON"，不是仓库代码。上传云端存一份，另一台设备拉下来覆盖本地。

**技术方案**：
- 云端：Cloudflare Workers + KV（免费额度约 10 万请求/天，个人工具够用），3 个接口：
  - `PUT /sync` 上传（带 license + 设备标识 + 配置 JSON，校验后写 KV）
  - `GET /sync` 拉取（按账号返回配置）
  - 冲突处理：比较最后修改时间戳，云端保留最新（后写覆盖）
- 客户端（Rust 端）：
  - 新增 `sync_push`：读 state/groups/favs 三个 JSON → 打包 → HTTP POST
  - 新增 `sync_pull`：GET 拉回 → 分别写回三个 JSON → 前端 reload 配置
  - 依赖：`reqwest` 或复用 `tauri-plugin-http`
- 前端：设置区加"云同步"面板——license 状态、已绑定设备数、最后同步时间、`立即同步`按钮
- License 与设备数：购买后发码，应用内激活；首次同步注册设备，Worker 按 license 限制设备数（免费版限 1 台，Pro 解锁多设备）；校验放 Rust 端，防前端被改

**遗留问题（待有真实用户后再定）**：
- 跨设备仓库路径可能不同（如 `/Users/xxx/work` vs `/Users/xxx/code`），最小版先原样同步，后续可做"路径映射"

**商业化定位**：若未来做收费，云同步作为 Pro 收费点（开源本地版给不了的能力），免费版限 1 台设备。

---

## 商业化方向（尚未启动，仅供参考）

- 现状：已在 GitHub 开源（v0.2.0 全平台），当前建议以开源积累口碑/作品集为主
- 若收费，推荐"免费基础版 + Pro 买断制"，Pro 只做闭源的云增值能力（见上），本地功能保持免费开源
- 支付：Gumroad / Lemon Squeezy（自动发码，抽成约 5%）；国内爱发电/面包多
- 起步路径：开源积累 → 云同步最小版 → 接支付 → 早鸟价验证（50 个真实用户里是否有 3-5 人付费，没有就停）
